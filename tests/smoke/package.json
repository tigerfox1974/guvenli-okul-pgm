{
  "name": "guvenli-okul-pgm-smoke",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only browser smoke tests for Güvenli Okul PGM.",
  "scripts": {
    "install:browsers": "playwright install chromium",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
